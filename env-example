DB_USER="NEW USER"
DB_PASSWORD="NEW PASSWORD"
DB_HOST="HOST"
DB_PORT="PORT"
DB_NAME="DB NAME"


#.env-migrations (archivo env.py modificación para conexion en base de datos).
linea 5: from app import db, metadata (pegar esto)
linea 39: target_metadata = db.metadata (pegar esto)
Esto permite la conexion y migracion de tablas
